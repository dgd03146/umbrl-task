import FeatureDisplay from './FeatureDisplay';
import FeatureList from './FeatureList';

const Features = () => {
  return (
    <section>
      <FeatureDisplay />
      <FeatureList />
    </section>
  );
};

export default Features;
